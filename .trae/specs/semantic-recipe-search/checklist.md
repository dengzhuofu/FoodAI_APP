\*- \[x] 后端环境安装 `langgraph`, `sentence-transformers`, `faiss-cpu`, `langchain-community`

* [x] `VectorService` 成功索引食谱和知识库

* [x] `KitchenAgent` 能够使用 `retrieve_cooking_knowledge` 回答烹饪问题

* [x] `backend/app/agents/planner_graph.py` 实现多节点工作流

* [x] `POST /api/ai/plan-event` 能够返回结构化的活动计划（菜单+购物清单）

